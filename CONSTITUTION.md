# go-skeleton 项目宪法

> 仓库最高约束,对人和 AI 同等生效。任何与其冲突的文档、代码、口头约定,一律以本文件为准。

## AI 工作原则(与条款同等效力)

1. **编码前思考**:不假设。假设要明说;有歧义呈现多种解释再问;有更简单的做法就提出;困惑时停下来问,不要默默选一种解释执行。
2. **简洁优先**:只写要求的东西。不加未要求的功能、灵活性、可配置性;一次性代码不抽象;200 行能写成 50 行就重写。检验:资深工程师会不会觉得过度复杂?
3. **精准修改**:只碰必须碰的。不"顺手改进"相邻代码、注释、格式;匹配现有风格;无关的死代码只提出、不删除;但自己的改动产生的孤儿导入/变量必须清掉。检验:每行 diff 都能追溯到用户请求。
4. **目标驱动**:把任务转成可验证目标再动手——"修 bug"→"先写重现测试,再让它通过";"重构"→"前后测试全绿"。多步任务先列步骤与每步的验证方式。

## 条款(违反任意一条,改动不被接受)

1. **错误必须上抛**:error 逐层返回给调用方;禁止吞错、禁止只打日志不返回、禁止业务路径 panic。panic 只允许出现在装配期(加载配置、构造组件),那里「起不来就该死」;失败原因不限于配置写错,端口占用、依赖不可用、环境不支持同样适用。
2. **错误双通道**:业务错误用 `errcode.Code`,翻译底层错误必须 `errcode.Wrap(cause, ec)`;原始错误只进日志,绝不进给客户端的消息。
3. **公共库克制**:`pkg/` 下不允许出现任何业务概念;通用错误码只占用 10000–19999 分段,业务码归属各服务分段。
4. **零硬编码**:服务运行时配置只来自 `configs/*.yaml`——yaml 即配置,不同环境下发不同文件;代码中不出现魔法地址/密码/端口。构建与运维工具(迁移等)的参数不属服务配置,不在此列。
5. **提交门槛**:`make check` 全绿(build / vet / test 三条 + gofmt 零漂移),否则任务不算完成。
6. **文档同步**:改了被 CONSTITUTION.md / AGENTS.md / `.agents/` 描述的行为,同一次改动里更新对应文档。

## 修订

宪法的增删改是仓库级决策:单独提交、单独评审,不允许夹带在功能改动里;修订后同步检查 `AGENTS.md` 与 `.agents/` 是否出现冲突表述。
