# 宪法

本文件是本仓库 AI 协作的最高规则，每会话必须最先完整加载（Claude Code 经入口 `@` 自动导入；其他工具按 AGENTS.md 指引直接阅读）。条文求少求稳，修订须经用户明确批准。

**位阶**：用户明示指令 > 本宪法 > AGENTS.md（入口与路由）> `.agent/` 规范文档 > 通用惯例与工具默认行为。发现上下位冲突，先报告冲突再执行上位。

## 第一条 协议即事实源

对外接口只允许在 `api/**/*.proto` 中定义。一切生成物（`*.pb.go`、`*_grpc.pb.go`、`*.pb.gw.go`、`openapi/`）只能由构建链重新生成，禁止手改。

例外：基础设施探针与元端点（如 `/healthz`）不属于对外业务接口，允许在 gateway 上以代码注册，但必须列入 `.agent/architecture.md`「横切关注点」的元端点清单，不得擅自扩散。

## 第二条 按需强制加载

按 AGENTS.md 的文档路由表取用文档，不预读全库。任务命中路由行时，先读该行文档再动手——未读即着手视为违宪；任何行都不命中时走第三条。路由表视同宪法附件，修改须经用户批准。

## 第三条 未覆盖先问

规范与架构未覆盖的决策——新增第三方依赖、新增顶层目录、改变依赖方向、对外协议破坏性变更——先停下，向用户给出带推荐项与理由的方案，批准后再动手。

## 第四条 一致性义务

发现代码、文档、现实三者之间的任何不一致（含文档与文档矛盾），当轮明说，且必须修其一（改代码或改文档），禁止沉默绕过或私自「兼容」。

## 第五条 验证后声明

任何「完成 / 通过 / 可用」的结论，必须先实际运行对应命令（构建、测试、生成）并出示结果；跑不了就说明原因，不得凭推断宣告成功。

## 第六条 破坏性变更显式化

删除或重编号 proto 字段、变更字段语义、收紧校验，都属破坏性变更：必须由 `buf breaking` 显式暴露，单独列明并经用户确认后才可落地。例外：尚无 main 基线的首次落库轮次可跳过 breaking，须在提交说明注明。

## 第七条 生成物随源提交

`.proto` 变更与其生成物必须在同一提交落库，保证仓库任意提交点上二者一致。
